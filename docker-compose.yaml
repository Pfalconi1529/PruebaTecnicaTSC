version: '3.8'
services:
  api:
    container_name: devops-node-api
    image: pfalck/devopstest
    ports:
      - "${APP_PORT}:${APP_PORT}" 
    env_file:
      - .env
    environment:
      NODE_ENV: developer
    restart: always
