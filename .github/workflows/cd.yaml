# Define el nombre del flujo de trabajo
name: CD / Deployment to Kubernetes

# Se dispara después de un push exitoso a 'main'
on:
  push:
    branches:
      - main
      - dev # 💡 Ahora se dispara también con push a la rama 'dev'
      
# El job principal para el despliegue
jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # Define un entorno de despliegue (opcional pero buena práctica)
    environment: production 
    
    steps:
      - name: Checkout Code
        # Obtiene el código, necesario para acceder a los manifiestos de Kubernetes
        uses: actions/checkout@v4
      - name: Set up Kubeconfig Context
        uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG_DATA }}
          
      - name: Deploy Kubernetes Manifests
        # Utiliza la acción de Kubernetes para aplicar los manifiestos YAML
        uses: azure/k8s-deploy@v4
        with:
        
          manifests: |
            kubernetes/app-config.yaml
            kubernetes/app-secret.yaml
            kubernetes/app-pv-pvc.yaml
            kubernetes/app-deployment.yaml
            kubernetes/app-service.yaml
          namespace: default 
          action: deploy # Esto ejecuta 'kubectl apply'
          
      - name: Verify Deployment Status
        # Verifica que el despliegue haya sido exitoso antes de terminar el workflow
        run: kubectl rollout status deployment/devops-app-deployment
